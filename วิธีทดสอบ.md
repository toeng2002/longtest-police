# วิธีทดสอบระบบ

## ขั้นตอนแรก: เปิดเซิร์ฟเวอร์

เปิด PowerShell ที่โฟลเดอร์โปรเจกต์ แล้วรัน:

```powershell
cd "h:\web exam\longtest-police"
node serve.js
```

จะเห็นข้อความ:

```
 เปิดเว็บทดสอบได้ที่:  http://localhost:8080

  บัญชีทดสอบ:
    admin / 123456   -> โหมดผู้ดูแลระบบ
    test  / 123456   -> โหมดผู้ใช้

  กด Ctrl+C เพื่อหยุดเซิร์ฟเวอร์
```

ถ้าพอร์ต 8080 ถูกใช้อยู่ ให้เปลี่ยนพอร์ต:

```powershell
node serve.js 8081
```

## ขั้นตอนที่สอง: เปิดเบราว์เซอร์

เปิด **http://localhost:8080**

> **ห้ามดับเบิลคลิก `index.html`** เปิดเป็นไฟล์ตรง ๆ (`file:///...`)
> เพราะเบราว์เซอร์จะบล็อกการเรียก Supabase ด้วยนโยบาย CORS
> ทำให้ปุ่มล็อกอินขึ้นว่า "เชื่อมต่อฐานข้อมูลไม่ได้"

## บัญชีที่ใช้ทดสอบ

| Username | Password | เข้าโหมด |
|---|---|---|
| `admin` | `123456` | ผู้ดูแลระบบ (dashboard, จัดการข้อสอบ) |
| `test` | `123456` | ผู้ใช้ (ทำข้อสอบ, ดูประวัติ) |

## ลำดับการทดสอบที่แนะนำ

### 1. ทดสอบล็อกอิน
- กรอก `admin` / `123456` → ต้องเข้าโหมดแอดมิน
- กดออกจากระบบ แล้วลองกรอก `admin` / `ผิด` → ต้องขึ้นข้อความไม่ถูกต้อง
- กด Enter ในช่องรหัสผ่าน → ต้องล็อกอินได้ (ไม่ต้องคลิกปุ่ม)

### 2. ทดสอบโหมดผู้ใช้ (ล็อกอินด้วย `test`)
1. เลือกหน่วย → **ตม.**
2. เลือกระดับ → **ชั้นประทวน**
3. เลือกรูปแบบ → **แยกรายวิชา**
4. เลือกวิชา → **กฎหมาย ตม.** (จะแสดงจำนวนข้อ)
5. เลือกโหมด → **ฝึกซ้อม**
6. กด **เริ่มทำข้อสอบ**
   - ตอบข้อแรก → ต้องเห็นเฉลยทันที
   - กด **ถัดไป** → ทำจนครบ
7. ดูผลคะแนน → ต้องมีคะแนน, สถิติ, เฉลยและคำอธิบาย
8. กด **กลับหน้าหลัก** → กด **ประวัติ** → ต้องเห็นรายการที่เพิ่งทำ

### 3. ทดสอบว่าโจทย์สุ่มจริง
- ทำชุดเดิมซ้ำ 2 ครั้ง → ลำดับข้อและลำดับตัวเลือกต้องไม่เหมือนเดิม

### 4. ทดสอบโหมดแอดมิน (ล็อกอินด้วย `admin`)
- **Dashboard** → ตัวเลขต้องมาจากฐานข้อมูลจริง (ตอนนี้จะเห็น 0 เพราะตารางว่าง)
- **ข้อสอบทั้งหมด** → ตารางจะว่าง (ยังไม่มีข้อมูลใน DB)
- **เพิ่มข้อสอบ** → ลองกรอกแล้วกดบันทึก
- **นำเข้า CSV** → หน้านี้เป็นตัวอย่าง UI ยังไม่ทำงานจริง

## ข้อมูลในฐานข้อมูล
### ใส่หน่วยงาน + วิชาเริ่มต้น (ทำแล้ว)

```powershell
node seed.js
```

สคริปต์นี้ใส่ **6 หน่วยงาน** และ **7 วิชา** ลงตาราง `units` และ `subjects`
ถ้ารันซ้ำจะขึ้น error เพราะข้อมูลมีอยู่แล้ว (ไม่เป็นอันตราย)

**หน่วยงานที่ใส่:** ตม. ส. จร. ป. อก. พยาบาล
**วิชาที่ใส่ (เฉพาะหน่วย ตม.):**

| ระดับ | วิชา | สัดส่วน |
|---|---|---|
| ชั้นประทวน | กฎหมาย ตม. | 40% |
| ชั้นประทวน | ภาษาไทย | 30% |
| ชั้นประทวน | ความรู้ทั่วไป | 30% |
| ชั้นสัญญาบัตร | กฎหมาย ตม. | 35% |
| ชั้นสัญญาบัตร | ภาษาอังกฤษ | 25% |
| ชั้นสัญญาบัตร | กฎหมายอาญา | 25% |
| ชั้นสัญญาบัตร | ความรู้ทั่วไป | 15% |

### โครงสร้างตารางจริง (ยืนยันจาก error ของ Postgres)

```
units     → id (text, PK), name, icon, short_name
subjects  → id (auto), unit_id, name, level, ratio
```

`level` ใช้ค่า `p` = ชั้นประทวน, `s` = ชั้นสัญญาบัตร, `both` = ทั้งสองระดับ
> ระวัง: ตาราง `units` **ไม่มี**คอลัมน์ `short` และตาราง `subjects` **ไม่มี**คอลัมน์ `icon`

### สถานะปัจจุบัน
| ตาราง | จำนวนแถว |
|---|---|
| `units` | 6 |
| `subjects` | 7 |
| `questions` | 0 |
| `question_units` | 0 |

**ยังไม่มีข้อสอบ** — ตอนเพิ่มข้อสอบจะเลือกวิชาได้แล้ว แต่ต้องเพิ่มวิชาของหน่วยอื่น
ก่อนถ้าจะออกข้อสอบหน่วยนั้น
**การทำข้อสอบยังใช้ได้** — มีชุดข้อสอบสำรองใน `js/exam-db.js`
(ประทวน 18 ข้อ / สัญญาบัตร 18 ข้อ เฉพาะหน่วย ตม.) ระบบจะใช้ชุดนี้
automatically เมื่อตาราง `questions` ว่าง
**หน่วยอื่นนอกจาก ตม.** — ชุดสำรองมีแค่ ตม. ถ้าเลือกหน่วยอื่น
จะขึ้นว่า "ไม่พบข้อสอบในชุดนี้" จนกว่าจะเพิ่มข้อสอบลงฐานข้อมูล
### เพิ่มวิชาของหน่วยอื่น
แก้ไข array `SUBJECTS` ใน `seed.js` แล้วรัน `node seed.js` อีกครั้ง
หรือเพิ่มผ่าน Supabase Dashboard → Table Editor → `subjects`

## ถ้าเจอปัญหา

| อาการ | สาเหตุ | วิธีแก้ |
|---|---|---|
| "เชื่อมต่อฐานข้อมูลไม่ได้" | เปิดเป็น `file://` | รันผ่าน `node serve.js` |
| หน้าขาว ไม่มีอะไรเลย | สคริปต์โหลดไม่ครบ | กด F12 → แท็บ Console ดู error |
| ล็อกอินไม่ผ่าน | รหัสผิด | ใช้ `123456` |
| ข้อสอบไม่ขึ้น | หน่วยที่เลือกไม่มีข้อมูลสำรอง | เลือกหน่วย **ตม.** |
| พอร์ตถูกใช้ | มีเซิร์ฟเวอร์อื่นอยู่ | `node serve.js 8081` |

## เปิด Console ดู error

กด **F12** ในเบราว์เซอร์ → แท็บ **Console**
ถ้าทุกอย่างถูกต้อง จะไม่มีข้อความสีแดงเลย
